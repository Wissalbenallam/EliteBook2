@model TMS_Project.Models.Tournee
@{
    ViewData["Title"] = "Détails - Tournée";
}

<h3>Détails de la tournée #@Model.TourneeId</h3>

<table class="table">
    <tr><th>ID</th><td>@Model.TourneeId</td></tr>
    <tr><th>Date</th><td>@Model.DateTournee.ToString("dd/MM/yyyy HH:mm")</td></tr>
    <tr><th>Transporteur</th><td>@Model.Transporteur?.Nom</td></tr>
    <tr><th>Camion</th><td>@Model.Camion?.Immatriculation</td></tr>
    <tr><th>Chauffeur</th><td>@Model.Chauffeur?.Nom</td></tr>
    <tr><th>Coût total</th><td>@Model.CoutTotal.ToString("C")</td></tr>
</table>

<p>
    <a asp-action="Edit" asp-route-id="@Model.TourneeId" class="btn btn-warning">Modifier</a>
    <a asp-action="Index" class="btn btn-secondary">Retour</a>
</p>